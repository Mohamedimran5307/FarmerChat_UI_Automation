appId: any
name: "TC04 - Audio Response Feature"
tags:
  - home
  - chat
  - regression
---
- runFlow: ../../helpers/complete_onboarding.yaml

# Open the type input bar
- tapOn:
    id: "primary_input_type_btn"
- extendedWaitUntil:
    visible:
      id: "chat_text_input"
    timeout: 5000
- tapOn:
    id: "chat_text_input"
- inputText: "What are common tomato diseases in Kenya"
- extendedWaitUntil:
    visible:
      id: "chat_send_btn"
    timeout: 5000
- tapOn:
    id: "chat_send_btn"

# scrollUntilVisible waits up to 90s for chat_listen_btn, which appears
# only after the answer finishes streaming — no separate "wait for AI
# response" step needed.
- scrollUntilVisible:
    centerElement: true
    element:
      id: "chat_listen_btn"
    direction: DOWN
    timeout: 90000

# Start playback, then stop it — both should be no-ops on the same control
- tapOn:
    id: "chat_listen_btn"
- waitForAnimationToEnd
- assertVisible:
    id: "chat_listen_btn"
- tapOn:
    id: "chat_listen_btn"
- waitForAnimationToEnd
- assertVisible:
    id: "chat_screen"
