APP_ID: org.digitalgreen.farmer.chat
LANGUAGE: English (Kenya)
LANGUAGE_CODE: en
USER_NAME: Test Farmer
SHORT_NAME: TF
WAIT_TIMEOUT: 10000
# Secrets are loaded from config/env.local.yaml (gitignored). See
# config/env.local.example.yaml for the keys the suite expects. Anything
# missing at runtime will surface as ${VAR} unresolved in flow logs.
