# Copy this to config/env.local.yaml and fill with real values.
# env.local.yaml is gitignored — never commit it.
# Same KEY: VALUE format as env.yaml. Keys defined here override env.yaml.

PHONE_NUMBER: 0000000000
NEW_PHONE_NUMBER: 0000000000
OTP_CODE: 0000
